services:
  - type: web
    name: mon_chatbot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: rasa run --enable-api --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.25
